var viewModel = function(){	var self = this;		self.switchLanguage = function(data, event){		var ln = $(event.target).data('ln');		console.log(ln);		if(translator.swithLanguage(ln)){			location.reload();		}	};};$(function(){	var vm = new viewModel();	ko.applyBindings(vm);    var tt = translator.translate('title');	//language setting	$('.ln-switcher').each(function(){		if($(this).data('ln') == translator.currentLanguage() ){			$(this).hide();		}else{			$(this).show();		}	});})