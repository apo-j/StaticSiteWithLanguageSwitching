var sideBarItem = function(title, href){	var self = this;	self.title = translator.translate(title);	self.href = href;}var sideBar = function(items){	var self = this;		self.items = items || [];}var section = function(id, title, body){	var self = this;	self.id = id;	self.title = translator.translate(title);	self.body = translator.translate(body);}var sections = function(items){	var self = this;	self.items = items || [];}var part = function(id, on, _sideBar, title, _sections){	var self = this;	self.id = id;	self.title = translator.translate(title);	self.sections = _sections || new sections();	self.on = ko.observable(on);	self.sideBar = _sideBar || new sideBar();	self.hasSideBar = ko.computed(function(){		return self.sideBar.items.length > 0;	});}var viewModel = function(){	var self = this;		var aboutSideBar = new sideBar([		new sideBarItem('ABOUT', '#ddt'),		new sideBarItem('Index', '#ddd'),		new sideBarItem('title', '#kitty')	]);		var aboutSections = new sections([		new section('one', 'first', 'one'),		new section('two', 'second', 'two'),		new section('three', 'third', 'three')	]);		self.parts = ko.observableArray([		new part('index', true, null, 'PART_INDEX_TITLE', null),		new part('about', false, aboutSideBar, 'PART_ABOUT_TITLE',aboutSections)	]);		self.switchLanguage = function(data, event){		var ln = $(event.target).data('ln');		console.log(ln);		if(translator.swithLanguage(ln)){			location.reload();		}	};		self.toggle = function(data, event){		var part = $(event.target).data('part') || '';		ko.utils.arrayForEach(self.parts(), function (item) {            if(item.id === part){				item.on(true);			}else{				item.on(false);			}        });	};};$(function(){	ko.applyBindings(new viewModel());})