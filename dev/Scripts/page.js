var sideBarItem = function(title, href){	var self = this;	self.title = translator.translate(title);	self.href = href;}var sideBar = function(items){	var self = this;	self.items = items || [];}var section = function(id, title, body){	var self = this;	self.id = id;	self.title = translator.translate(title);	self.body = translator.translate(body);}var sections = function(items){	var self = this;	self.items = items || [];}var page = function(id, on, _sideBar, title, _sections){	var self = this;	self.id = id;	self.title = translator.translate(title);	self.sections = _sections || new sections();	self.on = ko.observable(on);	self.sideBar = _sideBar || new sideBar();	self.hasSideBar = ko.computed(function(){		return self.sideBar.items.length > 0;	});}var viewModel = function(){	var self = this;		var aboutSideBar = new sideBar([		new sideBarItem(enums.sideBarItemEnum.Introduction, enums.sideBarItemEnum.Introduction),		new sideBarItem(enums.sideBarItemEnum.Details, enums.sideBarItemEnum.Details)	]);		var aboutSections = new sections([		new section(enums.sideBarItemEnum.Introduction, 'INTRODUCTION', 'INTRODUCTION'),		new section(enums.sideBarItemEnum.Details, 'DETAILS', 'DETAILS')	]);		self.pages = ko.observableArray([		new page('index', true, null, enums.traductionCodeEnum.PageIndexTitle, null),		new page('about', false, aboutSideBar, enums.traductionCodeEnum.PageAboutTitle,aboutSections)	]);		self.switchLanguage = function(data, event){		var ln = $(event.target).data('ln');		console.log(ln);		if(translator.swithLanguage(ln)){			location.reload();		}	};		self.toggle = function(data, event){		var page = $(event.target).data('page') || '';		ko.utils.arrayForEach(self.pages(), function (item) {            if(item.id === page){				item.on(true);			}else{				item.on(false);			}        });	};};$(function(){	ko.applyBindings(new viewModel());})