var part = function(id, on, title, subs){	var self = this;	self.id = id;	self.title = translator.translate(title);	self.subparts = subs;	self.on = ko.observable(on) ;}var viewModel = function(){	var self = this;		self.parts = ko.observableArray([		new part('index', true, 'PART_INDEX_TITLE', []),		new part('about', false, 'PART_ABOUT_TITLE',[])	]);		self.switchLanguage = function(data, event){		var ln = $(event.target).data('ln');		console.log(ln);		if(translator.swithLanguage(ln)){			location.reload();		}	};		self.toggle = function(data, event){		var part = $(event.target).data('part') || '';		ko.utils.arrayForEach(self.parts(), function (item) {            if(item.id === part){				item.on(true);			}else{				item.on(false);			}        });	};};$(function(){	ko.applyBindings(new viewModel());})